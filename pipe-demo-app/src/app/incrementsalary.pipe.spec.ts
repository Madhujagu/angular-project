import { IncrementsalaryPipe } from './incrementsalary.pipe';

describe('IncrementsalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new IncrementsalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
